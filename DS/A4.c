#include<stdio.h>
#include<string.h>
#include<math.h>
#define size 30

char StackA[size];
int StackB[size];
int topA = -1;
int topB = -1;


int main() {
    int prifix[size];
    printf("Enter the prefix expression: ");
    scanf("%s", prifix);

    
}